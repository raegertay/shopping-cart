<div id="fixed-navbar">
<nav class=" navbar navbar-expand-lg navbar-light">

  <%= link_to root_path, class:'navbar-brand' do %>
  <%= image_tag "/assets/company_logo.png", height: '40', width: '60' %>
  <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">

    <ul class="navbar-nav w-100 justify-content-between">
      <li class="nav-item active">
        <a class="nav-link" href="<%= root_path %>">Home <span class="sr-only">(current)</span></a>
      </li>

      <li class="nav-item cart ml-auto">
        <%= render 'carts/cart' %>
      </li>

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle text-danger" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Menu
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <%= link_to 'Orders', orders_path, class: 'dropdown-item' if customer_signed_in? %>
          <%= link_to 'Edit Profile', edit_customer_registration_path, class: 'dropdown-item' if customer_signed_in? %>
          <%= link_to 'Login', new_customer_session_path, class: 'dropdown-item' unless customer_signed_in? %>
          <%= link_to 'Register', new_customer_registration_path, class: 'dropdown-item' unless customer_signed_in? %>
          <div class="dropdown-divider"></div>
          <%= link_to 'Logout', destroy_customer_session_path, method: :delete, class: 'dropdown-item' if customer_signed_in? %>
        </div>
      </li>
    </ul>

  </div>
</nav>
</div>
