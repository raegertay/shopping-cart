= link_to 'Logout', destroy_customer_session_path, method: :delete if customer_signed_in?
= link_to 'Login', new_customer_session_path unless customer_signed_in?

h1 Products index page for customers
